## 1. Evidence and protected seats

- [x] 1.1 Reproduce the outward radial-ball/ring contact natively and world64, inspect a section, and retain a desired red regression. Two kernel failures and the measured contact are recorded in the positioning-ball investigation.
- [x] 1.2 Verify the actual sphere, ring phase, radial/lift/shift frames and independently bounded passage; prove mapped collar and spring support material is outside it before fitting. Current-collar fixture matches the root; 205 lift/shift support probes avoid the independent region, upper plane is untouched, and actual root motion confirms the ring lifts while ball/ring do not follow carriage shift. Source sphere R3.75 and mesh maximum radius 3.750000116 mm measured.

## 2. Bounded candidate

- [x] 2.1 Implement the isolated underside passage and prove connected valid removal-only material, independent bounds, unchanged seats, fresh/built parity and a measurable named gap. Five material/enclosure/negative tests pass; .04/.05/.06 mm distances match their named gaps. Default removal is 2.4289162467 mm³.
- [x] 2.2 Prove full-path native enclosure and dense world64 clearance; rerun actual Follow contact and existing seat/capture contracts, including all relevant shifts and lifts. All 2,501 radial/lift samples clear in both representations; outward-rounded full-path capsule has zero native common. Actual Follow ring contact passes; five seat tests pass in each kernel.
- [x] 2.3 Prove restored/misplaced/excessive relief negatives and inspect the native section and supported assembly. Native negative controls pass; source/fit sections with collar and spring present are inspected. The intact-assembly render is inspected but its collar occludes the local gap, so it is not claimed as visible-clearance evidence.

## 3. Adoption and completion

- [x] 3.1 Adopt only the verified ring; compare every other part and original bank/law, perform paired neighbour inventories and relevant operating replays without waivers. Production root/Follow contact checks pass 2/2; actual production support/lift/shift/replay and all-neighbour rest checks pass 2/2 per kernel. Paired static addition inventory is unchanged; paired Follow addition removes exactly ball/ring and changes no other pair.
- [x] 3.2 Record bounded completion and outstanding ball-motion/whole-machine gates, validate and sync specs, archive, and commit the implementation. The local evidence and F29 record preserve the unadopted ball-motion and open whole-machine status; the synced capability and change validate strictly. No umbrella task is closed.
