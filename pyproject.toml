[tool.solid-node]
model = "operating_curta"

[tool.solid-node.models]
fast_curta = "simulation.curta:Curta"
operating_curta = "simulation.running:OperatingCurta"
clocked_curta = "simulation.clocked:ClockedCurta"

[tool.libresolid-studio]
profile = "builder"
