# Higher-counter bank: unadopted operating trial

The production hundreds regression prepares a partly driven input through
actual requests, withdraws it, and then requests crank 220°. The request
incorrectly completes into **.3222787565706012 mm³ native /
.3398941848482346 mm³ world64** upper/bell common. This is observed red
evidence (34.402 s, exit 1), not a guessed need for another restraint.

`CounterBankOperatingTrial` leaves the default root unchanged. It gives each
station 2..6 its own source-specific .16 mm upper-lockout trial and combines
five independently framed contact gaps into one additional crank lower bound.
The source phases differ by 20°; every higher counter retains its own raw
upper travel -1.8..2.4 mm. No result-bank axial conversion is applied to
counter parts. Existing result, counter-ones and pawl bounds remain in place.

The independent fixture passes (67.661 s, observed exit 0): all five complete
native upper prints, and the bell against each bench, match the corresponding
independently measured station exactly in both cut directions. The initial
213-coordinate bank matches the unchanged production root. The two numeric
bank-law tests pass (0.442 s), checking all five frame conversions and exact
pair count. An intermediate `counter-bank-law-green-2026-09-22.log` failed a
test typo (`higher_closing_limit` rather than `higher_counter_closing_limit`);
only `counter-bank-law-final-2026-09-22.log` is passing evidence.

The production regression harness now accepts a station number but preserves
the tens request order, timing, native/world64 checks, forced-contact negative
control, full-snapshot replay, relief, idle and retry. New production contracts
for stations 3..6 remain red until justified adoption. Each trial class writes
its own acceptance file when run separately; do not give a combined multi-class
run one output filename and mistake the last class's report for the bank.

The hundreds trial's full short/long check is running at this checkpoint.
The first 220° request stops at 214.7853836059494° with clear complete prints;
that partial progress is not a completed test. The other station action orders,
combined-bank arithmetic/browser checks and adoption remain owed. Independent
finite native and world64 profile gates are recorded in the
[counter continuation](counter-tens-continuation-2026-09-22.md).

Logs are retained in `_build_checks/`: `counter-hundreds-operating-production-red-2026-09-22.log`,
`counter-hundreds-operating-contact-red-2026-09-22.log`,
`counter-bank-law-{red,green,final}-2026-09-22.log`,
`counter-bank-complete-fixture-2026-09-22.log` and the separately pending
`counter-bank-hundreds-python-2026-09-22.*`. All use framework `acebc48`.
No OpenSpec task is closed, no upstream CAD asset is changed, and this trial
is not a fabrication recommendation.

## Hundreds withdrawal gate completed

The full hundreds trial completes in 312.870 s, observed exit zero. Both
220° and 940° requests stop at 214.7853836059494° /
214.78538360544917°, clear in native and world64 complete-print commons.
Forcing another .2° contacts in both kernels. Full snapshots replay exactly,
.05° relief succeeds, idle preserves it and retry blocks at the same stop.
All 213 stopped/idle coordinates are captured in the Python report for the
independent browser check. This retires the corresponding measured production
failure only in the isolated trial, not by adopting the remaining stations.

The ordinary-height world64 profile matrix also completes for all five higher
counters: **80,710 admitted poses, zero contacts**, every process observed
exit zero. Station 2's run used the earlier support law; its later axial
correction only removes admissions. Stations 3..6 use the corrected law.
Their precise source headers remain in the
[world64 matrix](evidence/counter-bank-full-world64-completion-2026-09-22.json),
beside the separate corrected intermediate-height support evidence. No
continuous or full action-order claim follows from those finite matrices.

The independent hosted browser hundreds check now also completes with observed
exit zero. It uses export identity `4127d33c87f1501a024ee5c7391d1897ce3af65309b946c811d4244c7fd68da2`
and bundle `f1f2cd4ce7ea630b95044631d2b53435dae1114b6c2c72321a381df5266570f7`,
loaded before the later Bound-demand viewer merge. Both stops and all 213
stopped/idle coordinates match the captured Python report exactly; snapshot
replay, relief and retry pass without page errors. The final screenshot was
inspected. This is hosted API coverage, not a pointer gesture matrix or
acceptance of the later viewer bundle. The
[hundreds evidence](evidence/counter-bank-hundreds-operating-2026-09-22.json)
pins the reports, image and red/green validator results (8 tests passing).
The complete-bank arithmetic batch and sixth-station operating gate are
running; neither is claimed complete here.

## Complete Python bank acceptance

The other four station processes now finish with exit zero on framework
`acebc48`: tens 256.609 s, fourth 262.897 s, fifth 263.642 s and sixth
265.786 s. Each runs both short/long requests, complete native/world64 print
commons, forced-contact negative controls, exact snapshot replay, relief,
idle and retry. Their first stops are respectively 194.7853836059494°,
234.7853836059494°, 254.7853836059494° and 274.7853836059494°.
Together with the hundreds check above, these cover all five higher counters
in the combined trial, not five independently substituted single-station roots.

Both complete six-test arithmetic batches pass unchanged: `acebc48` takes
752.962 s and the integrated Bound-demand correction `4bff28e` takes
405.283 s. These are acceptance timings under concurrent work, not a controlled
performance comparison. They include every independent selector, successive
addition, manual carry calibration, partial-release/replay, shifted dial
association, and full-register subtraction/undo.

The retained `_build_checks/` artifacts and SHA-256 values are:

| Artifact | SHA-256 |
| --- | --- |
| `counter-bank-arithmetic-acebc48-2026-09-22.log` | `cb6e5fb1282aa66cea03071986aa244d596e1180b029ab86ef6d4582c5b5c577` |
| `counter-bank-arithmetic-4bff28e-2026-09-22.log` | `0c219642f4859e8dbd1300574df2a00a3a120296d8c4680ddac617f108bcfc63` |
| `counter-bank-tens-python-2026-09-22.json` | `a159d0b88c90ea5437d441d7ab97c89b2958699591438f9969d6ce68ec4c7063` |
| `counter-bank-fourth-python-2026-09-22.json` | `51c95431d3cce9e6fc088742f51603de1eae8b5cddce24e6b312cc3df28f9a0d` |
| `counter-bank-fifth-python-2026-09-22.json` | `5e1cd6ba3236b32a176530b81cc109c5ca6eca143f97c1e62cc6e1db86be800f` |
| `counter-bank-sixth-python-2026-09-22.json` | `233ea6f916329821b3fabd98e0a3c0146177feafe58eaf7c6d91b15e0319d2e1` |

The sixth-station hosted browser run on bundle
`8a03c3a3d3cb83c132059474092a15e7b2a085871d98d87cdcccbd40566bfe81`
finishes both requests at the same stops, with replay/relief/retry correct and
no page errors, but **fails** the exact Python-bank gate. One coordinate,
`carriage.registers.dial_detents.p_6mm_ball_419241_9.lift`, is
0.08929057589867746 in the browser versus 0.0892905758986775 in Python in
both stopped and idle banks. The other 212 coordinates agree exactly.
Its report in `_build_counter_bank_sixth_c895dba/` remains `pending`, not
passed. The difference is under investigation; neither an epsilon nor a
geometry waiver is inferred from its small size.

Combined-root regressions for the already adopted counter-ones and result
station 3/8 withdrawals are added unchanged through test subclasses and are
running separately. The trial is still unadopted, and no OpenSpec task closes.
