# Higher-counter bank: unadopted operating trial

The production hundreds regression prepares a partly driven input through
actual requests, withdraws it, and then requests crank 220°. The request
incorrectly completes into **.3222787565706012 mm³ native /
.3398941848482346 mm³ world64** upper/bell common. This is observed red
evidence (34.402 s, exit 1), not a guessed need for another restraint.

`CounterBankOperatingTrial` leaves the default root unchanged. It gives each
station 2..6 its own source-specific .16 mm upper-lockout trial and combines
five independently framed contact gaps into one additional crank lower bound.
The source phases differ by 20°; every higher counter retains its own raw
upper travel -1.8..2.4 mm. No result-bank axial conversion is applied to
counter parts. Existing result, counter-ones and pawl bounds remain in place.

The independent fixture passes (67.661 s, observed exit 0): all five complete
native upper prints, and the bell against each bench, match the corresponding
independently measured station exactly in both cut directions. The initial
213-coordinate bank matches the unchanged production root. The two numeric
bank-law tests pass (0.442 s), checking all five frame conversions and exact
pair count. An intermediate `counter-bank-law-green-2026-09-22.log` failed a
test typo (`higher_closing_limit` rather than `higher_counter_closing_limit`);
only `counter-bank-law-final-2026-09-22.log` is passing evidence.

The production regression harness now accepts a station number but preserves
the tens request order, timing, native/world64 checks, forced-contact negative
control, full-snapshot replay, relief, idle and retry. New production contracts
for stations 3..6 remain red until justified adoption. Each trial class writes
its own acceptance file when run separately; do not give a combined multi-class
run one output filename and mistake the last class's report for the bank.

The hundreds trial's full short/long check is running at this checkpoint.
The first 220° request stops at 214.7853836059494° with clear complete prints;
that partial progress is not a completed test. The other station action orders,
combined-bank arithmetic/browser checks and adoption remain owed. Independent
finite native and world64 profile gates are recorded in the
[counter continuation](counter-tens-continuation-2026-09-22.md).

Logs are retained in `_build_checks/`: `counter-hundreds-operating-production-red-2026-09-22.log`,
`counter-hundreds-operating-contact-red-2026-09-22.log`,
`counter-bank-law-{red,green,final}-2026-09-22.log`,
`counter-bank-complete-fixture-2026-09-22.log` and the separately pending
`counter-bank-hundreds-python-2026-09-22.*`. All use framework `acebc48`.
No OpenSpec task is closed, no upstream CAD asset is changed, and this trial
is not a fabrication recommendation.
