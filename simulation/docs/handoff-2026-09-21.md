# Operating Curta — fresh-agent handoff

## Assignment and checkpoint

The pilot's objective is **finish the operating Curta**, not just its latest
framework correction. On 2026-09-21 the pilot requested a clean checkpoint
and a fresh agent because this has become a long investigation. Work is
paused for that handoff, **not finished or mechanically blocked**. Continue
task by task without asking the pilot to approve ordinary already-authorized
implementation and validation. Ask only for genuinely new consequential
design/framework scope. Do not re-propose or redo completed engine fixes.

The active project change is `simulate-the-curta`, **12/23 tasks complete**.
No whole-machine task was closed in this checkpoint. The default manifest
still selects `simulation.running:OperatingCurta`; the corrected T07 tens
restraint is still **only in HigherOperatingTrial**. The next concrete task
is its production adoption, subject to neighbouring arithmetic acceptance.

No Curta acceptance process remains running at this handoff. The unfinished
arithmetic process was deliberately interrupted (exit 130), not left for the
new agent to locate. Completed jobs/logs are listed below. No push, release,
PR or contributor contact was performed.

## Repositories and working environment

- Shop entry point: `/home/asa/devel/machinome-studio`.
- Actual project repository: `/mnt/data/machinome-projects/Calculators/Curta-Type-I-3x`,
  branch `direct-operation`. Shop path `projects/Calculators/Curta-Type-I-3x`
  resolves here. Verify `git rev-parse --show-toplevel` before writes/commits.
- Actual framework checkout: `/home/asa/devel/machinome-studio/machinome`
  (**not** the stale `machinome-framework` spelling in some instructions).
  Tested main: `9016f00d80ebec651f9a8db505441445a06b42b1`.
- Viewer: `/home/asa/devel/machinome-studio/machinome-viewer`.
  Tested main: `a1ae12dc3ca7c09638898c22501542b123b78acc`.
- Workspace executables: `/home/asa/devel/machinome-studio/.venv/bin/python`
  and `/home/asa/devel/machinome-studio/.venv/bin/machinome`.

Read the shop operating contract and applicable simulate-project/OpenSpec
apply/API/craft skills for a fresh agent. This is **direct project work**,
not floor-agent orchestration and not a shop sprint. Keep project changes in
this repository. New framework work must use the framework-change skill,
isolated framework worktree and its own ratified cycle. Viewer changes belong
to their separate repository. Do not use Bailian or spawn ad-hoc agents.

Preserve user files: the untracked assembly MP4 and
`screenshots/reverser_inspection.png` in this project. The shop also contains
unrelated user files/other ongoing work. Do not clean ignored build directories:
they contain large geometry caches and reproducible evidence.

Typical Python/geometry environment, from the project root:

```sh
ulimit -v 8388608
export OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1
export PYTHONPATH="/home/asa/devel/machinome-studio/machinome:$PWD"
export SOLID_BUILD_DIR=_build_checks
/home/asa/devel/machinome-studio/.venv/bin/python -m unittest <test> -v
```

Use a separate shell **without the virtual-memory cap** for browser runs.
Use ignored `_build_checks` or another named export directory, not the
pilot's live Studio build. Full-root tests genuinely take many minutes:
CPU-active computation with no new log lines is not evidence of a hang.
Avoid duplicate background jobs. No local libraries need upgrading.

## What has just been proved

Prior committed project content: `1211ccf` (integrated engine acceptance) and
`bcf9673` (refined contact supports). This handoff commit adds the final trial
results and red regressions; it does **not** adopt production geometry.

Start with [higher-support refinement](higher-support-refinement-2026-09-21.md)
and its [checkpoint evidence](evidence/handoff-checkpoint-2026-09-21.json).

- Coarse carry-tooth support intervals falsely jammed ten physically clear
  near-edge poses. All 110 independent support-edge measurements refine the
  table; original .1-degree crank stand-off and .002-degree shaft guard stay
  unchanged. No collision-volume epsilon, carry law or printed geometry was
  changed for this correction.
- The profile passes **8,063 complete-print samples per kernel**, **79,310
  ordinary source-path samples**, near-support intermediate carry positions,
  retained stops/replay/relief and a disabled-contact-strip mutation.
- The full source-backed HigherOperatingTrial passes its two raised/carried
  stop tests (**1215.666 s**) and the new real-carry/free-support test
  (**657.766 s**). These are completed results, not still-running jobs.
- Its fresh browser export passes actual carry preparation, raised/carried
  short and long stops, exact replay, relief/idle and the formerly false-jam
  request. **All 213 coordinates match exactly at all four stopped/idle
  states** between Python and browser. No page errors. Three full-model
  screenshots were inspected; isolated contact close-ups were inspected in
  the preceding checkpoint. Pixels do not substitute for contact geometry.
- Raised stop: `145.22323837279146`; carried stop:
  `504.9514572141925`. Free-support request reaches `506.3` and replays.

Current full-tree evidence:

```text
_build_checks/higher-t07-refined-full-operating.log
_build_checks/higher-t07-refined-full-free.log
_build_checks/higher-refined-python-acceptance.json
_build_checks/higher-t07-refined-full-browser.log
_build_higher_refined_operating/manifest.json
_build_higher_refined_operating/carry-browser-acceptance.json
_build_higher_refined_operating/carry-preparation.png
_build_higher_refined_operating/carry-withdrawal-relieved.png
_build_higher_refined_operating/carry-free-support.png
```

Document SHA-256:
`15617702eb301b7b00f6e88448fabe4886d565c0d1977ae92aa22da3990b1b62`.
Viewer bundle SHA-256:
`1098d52b62445f2a8ef6fece5ce38b4d9723ae1f8a6c74d21729668d4bb6d1aa`.

## Immediate next implementation: adopt tens without duplicating its bound

1. Finish the neighbouring arithmetic gate on HigherOperatingTrial:
   `test_manual_calibration_carries`,
   `test_subtraction_borrows_through_both_registers_and_addition_undoes_it`,
   `test_independent_inputs_and_two_successive_additions` from
   `simulation.test_running.RunningCurtaTest`. Run these against the trial
   with `unittest.mock.patch.object(test_running, 'OperatingCurta',
   HigherOperatingTrial)`, not a persistent edit of the original tests.
   `_build_checks/higher-t07-trial-arithmetic.log` is **interrupted during
   the first test, zero completed tests, exit 130**. No pass or defect can
   be inferred from that run. Preserve its provenance and rerun with a new
   log name. Other arithmetic previously passed on the old production root;
   that is not acceptance of this new bound.
2. The production red test is ready:
   `simulation.test_operating_higher_lockout.OperatingHigherLockoutTest`.
   It inherits the three real full-root cases using `model = OperatingCurta`.
   Its raised-withdrawal case ran before implementation and failed
   **completed instead of blocked, 66.318 s**:
   `_build_checks/higher-t07-production-red.log`.
   The other two production cases have not yet been run.
3. Extract or reuse the tested parts from `higher_lockout_trial.py`:
   `TrialTensLockout` uses **.16 mm** outer relief, vs the current .15;
   `TrialTensUpper` preserves its original keyed core/height;
   `TrialTens` replaces only the actual tens upper printed child;
   `TrialContactBell` refines mesh to `.01/.1`, leaving native material
   unchanged. A clean production-owned parts module with historical aliases
   is reasonable, but **no such extraction has been applied**.
4. In `running_parts.py`, adopt that tens class in `ResultShafts.tens` and
   the refined bell through `RetainedCarries.tens_bell`'s assembly. In
   `running.py`, add the same `higher_closing_limit` bound currently used
   in `HigherOperatingTrial` to the actual `main_drive.crank.turn` path.
   Read actual bell turn, retained tens turn and
   `transmission.result.tens.p_10220_410003_1_419227.travel`.
   Preserve all existing pawl/ones constraints, tree paths and controls.
5. Remove the trial's duplicate declaration once it inherits the production
   bound. Keep the historical diagnostic entry point and source-fit bench
   meaningful. A proposed multi-file extraction patch was rejected by the
   patch tool before applying **anything**, immediately before handoff;
   do not assume any partial adoption exists.
6. Pass the production three-case test, neighbouring arithmetic, source/fit
   contracts and fresh export/browser acceptance. Compare full banks and
   existing tree/controls. The browser tool is
   `simulation.tools.higher_operating_browser --build <export>
   --python-report <report>`; `CURTA_ACCEPTANCE_REPORT=<path>` makes the
   two stop tests write their banks. The free-support test has no report
   entry; the browser independently checks its actual state/replay.
7. Update the author-review entry and adoption docs only after this scope
   passes; then keep working. This alone does not close tasks 6.2/6.3.

## New empirical lead: remaining result bank (unadopted)

`simulation/tools/result_bank_lockout_probe.py` now constructs source-backed
pose instruments for result stations 2–11. Every station retains its source
pivot, upper-print placement and carry stroke. To express measurements in
tens-local coordinates, station `s` uses shift `20*(s-2)`: actual shaft is
local shaft minus shift, actual crank is local crank plus shift. Both modes
use the native-equivalent refined bell mesh. `--trial` changes only the
locking outline to the T07 .16 candidate; it does **not** change production.

The source-fit survey ran 100 indexed poses (10 stations × 2 seats × 5 flats).
Every station shows contact at normalized shaft 200 degrees (flat 3):
about `0.00001705657 mm³` native in the raised seat and
`0.00005116970 mm³` lowered. The refined bell mesh sees raised contact,
but misses the tiny lowered contact. These are simulation-fit findings,
not proof that the author's working printed machine jams.

`simulation.test_result_bank_lockout.ResultBankContactTest` deliberately
keeps `trial = False`: its indexed-clearance test is **red with 30 failing
subcases, 24.374 s**. Do not mark it expected-failure or suppress the volumes.
The proposed trial must prove preserved core/height/connectivity, both locking
flanks, ordinary motion and full profile admission before any bank adoption.
Its whole-bank flank test is written but **not run**; the `.16` bank survey
and full-bank trial acceptance are **not run** either.

The generalized contact-reader helper was independently checked against the
original T07 tens fixture: **1/1, 5.046 s**, exactly equal native/faceted
volumes for four shaft/height pairs and four crank angles. The existing
`contact_reader` defaults are unchanged; optional paths support other source
stations. Logs:

```text
_build_checks/result-bank-indexed-before.log
_build_checks/result-bank-indexed-red.log
_build_checks/result-bank-helper-parity.log
```

Numeric rows and log hashes are in the committed checkpoint evidence. Shared
phase normalization is a hypothesis to verify per station, not permission to
copy the tens profile untested. Counter-bank heights/geometry differ again.

## Remaining whole-project work

The authoritative checklist is
`openspec/changes/simulate-the-curta/tasks.md`, not the length of this history.
Eleven unchecked items remain: **1.3, 5.1–5.5, 6.2–6.6**.

- Complete crank/selector/lift/reverser action-order coverage, higher result
  and counter restraints, measured clear/contact evidence, retained replay.
  Ordinary operation must not require direct register setters, a calculator
  macro or automatic preparation. `Time.running` remains run-owned history.
- Printed clearing-loop deployment is unresolved. Read
  `clearing-loop-investigation-2026-09-19.md`: manual p38 describes clipping
  onto two glued rivets, **not the metal Curta's release button**. T05/T06
  seat trials pass endpoints/capture but a 111-pose sweep still contacts the
  second rivet at -89..-78 and -70..-63 degrees; maximum about 3.0343 mm³.
  Contact is on the body-side mouth wall. Do not erase the swept wall and
  claim retention survives. First throat is 6.2 mm vs a 7.5 mm bearing, so
  actual snap compliance/assembly calibration needs an evidence-backed
  representation. No deploy/stow input is adopted.
- Whole-machine rest/frame acceptance remains red. Read
  `operating-rest-inventory-2026-09-20.md` and its committed JSONs: 390 rigid
  occurrences, 268 positive faceted pairs, 166 native-or-source-STL pairs,
  zero refused commons at the recorded checkpoint. These are findings,
  **not exclusions**. Larger remaining contacts include spider mount/collar,
  clearing cover/counter body and thrust ring/collar. Do not waive moving
  contacts or invent collision-volume tolerances.
- Finish physical-pointer tests in both standalone and ordinary hosted
  viewers, all controls/wrong orders/partial movements, layer navigation,
  focused moving/rest/interlock snapshots. Existing scoped pointer tests do
  not equal whole-machine acceptance.
- Replace stale operating demos/instructions, run full faceted and exact
  regressions, rebuild actual default document/artifacts, inspect pixels,
  update README/source modifications, sync/archive OpenSpec and commit only
  when every task's own gate is genuinely satisfied.

`author-review.md` is the consolidated source-discrepancy/fit agenda for the
pilot to review with the original author. All upstream STEP/STL files remain
unchanged. Keep manufacturing claims separate from simulation acceptance.

## Closed software detours: do not reopen without new evidence

`mixed-contact-engine-2026-09-21.md` records the already archived, integrated
framework/viewer correction: relative threshold landing direction and exact
zero-motion rounding during following contact. It preserved real refusals,
formats, tolerances and geometry. Framework main is `9016f00`, viewer main
`a1ae12d`; the two completed worktrees were removed cleanly, branches retained.
Framework suite: 3499 passes, 4 skips, 2033 subtests. Viewer: 1413 JavaScript
passes and 192 Python/browser passes with 20 subtests. Shared corpus: 28
scenarios, 25 machines, 401 steps. The current real-Curta trial passes those
integrated executors. There is **no outstanding framework permission request**.

Earlier ancestor-joint constraints and periodic first-contact fixes are also
integrated, with their own records. Do not mistake their historic requests
or provisional paragraphs in append-only investigation logs for current
blockers. The missing work now is the operating mechanical project.
