# Counter-tens continuation

Status: **unadopted**. Passing arithmetic and request/replay checks do not
override a failing complete-print contact check.

On project content `a3dde6f`, the independent full-root counter-tens trial
passes its fixture and withdrawal tests (2/2, 438.395 s, observed exit 0).
Both 200° and 920° requests stop near 194.785383606°, clear in native and
world64 mesh commons; forcing another .2° produces positive contact in both.
Every snapshot replays exactly, .05° relief succeeds, idle preserves it, and
the retry stops again. The unchanged six-test arithmetic suite also completes
(1,744.637 s, observed exit 0), with the trial factory substituted but no
changed expected result, timing, request or arithmetic law.

The fresh browser export has 213 coordinates and 25 physical controls.
Both public running requests, snapshot replays, relief and retry pass with no
page errors. A separate post-run comparison matches every stopped and idle
coordinate against Python exactly, for both targets. The screenshot was
inspected. These are hosted API requests, not real-pointer coverage. The
original browser report honestly retains `python_report: null`; the later
comparison is a separate logged check, not rewritten history.

These checks use the pre-clearance law hash pinned in the
[evidence index](evidence/counter-tens-pre-support-clearance-2026-09-22.json).
The fresh world64 support-height sweep rejects that candidate: **96 positive
contacts in 5,352 admitted poses**, all at carry .2857142686843872, travel
-.6000000715255738. The old world32 sweep had rounded away these contacts.
The complete failed rows are retained in the
[rejection fixture](evidence/higher-counter-world64-support-rejection-2026-09-22.json).
They are not waived by a volume tolerance. Later law revisions require their
own acceptance and do not retroactively change this result.

## Lower support correction, still a candidate

The world64 lower-disc face is Z-8.399999618530272 rather than the native
Z-8.4. At the rejected carry height, the lockout bottom is
Z-8.399999928474426: their mesh common has positive thickness, not merely
coplanar triangles. A separate two-kernel axial bracket places mesh contact
entry at travel [-.6000005722045898, -.6000003218650818], while the native
kernel reports its transition slightly above -.6. Neither result is used to
erase the other's positive volume.

The candidate lower sector now begins refusing at travel **-.601 mm**, a
named .001 mm axial stand-off. That is the existing measured free-side probe,
not an overlap epsilon. Complete prints are clear at -.602 and -.601 in both
kernels; the old rejected pose and -.6 are now refused despite native zero
and mesh positive results. No body is cut, omitted or made smaller. Upper
and carry-tooth support formulas are unchanged. Raising this one lower-sector
gap can only remove admissions, never add a formerly refused pose.

The regression first fails all 96 saved contacts. After the correction,
all 15 law/probe/browser-validator tests pass, including the existing normal
input and carry-path checks. The same 30-height/18-shaft world64 support
sweep completes with **5,244 admitted checks, zero contacts**, observed exit
0. The 108 removed admissions include every one of the 96 previous contacts.
The [clearance record](evidence/counter-lower-support-clearance-2026-09-22.json)
pins both bracket measurements and complete-print free/contact poses. An
intermediate log named `counter-world64-support-law-green-2026-09-22.log`
actually failed two old test doubles after precision became explicit; it is
retained as a failed run, not acceptance. Updated doubles require world64.
Revised native support, operating, arithmetic and browser checks remain owed;
none of this adopts counter tens or declares continuous geometric coverage.

## Native ordinary-height profiles recovered with terminal exits

Fresh native runs now complete at all five higher-counter stations: 15,714
admitted checks each at stations 2/3/4, 16,538 at station 5 and 17,030 at
station 6, **80,710 total**, zero contacts and observed exit zero. These
processes loaded the pre-clearance law hash `a824d339...` and use carry
0/.5/1; their source headers and exact digests are in the
[fresh profile index](evidence/fresh-bank-profile-completion-2026-09-22.json).
The new station-2 run replaces the earlier incomplete process-supervisor
evidence, not its retained historical log. Native results do not settle the
world64 intermediate-height failure; the explicit lower-support correction
and its separate gates above remain necessary.
