# Remaining result-bank production adoption

The default `OperatingCurta` now uses the measured .16 mm locking-outline fit
at result stations 3..11 and their combined crank restraint. Ones and tens,
the pawl, and counter ones keep their separate restraints. Each new station
keeps its source-specific upper print, pivot, lower shaft and carry datum;
there is no shared substitute print or reparenting. No upstream asset changes.
This is a simulation fit, not a manufacturing recommendation or a finding
that the author's physical calculator fails.

The pre-adoption operating hundreds-withdrawal test fails: the requested
190° is completed with 0.308686232 mm³ native and 0.323535570 mm³ faceted
common. The production-part import and explicit source/production selection
regressions also fail before implementation. Their logs are retained.

`result_bank_lockout_parts.py` separates the production parts from the probe.
The independent trial remains a fixture, while the probe now defaults to
the actual production part. Its explicit `--source` option preserves the old
.15 mm reference, preventing a production-to-itself bounded-removal test.
Station identity distinguishes all nine artifact keys. Complete native upper
prints equal the independently measured trial in both difference directions;
all nine are valid single solids. Three part tests pass (10.196 s).

The actual root and the independent full-tree fixture both pass the installed
world-print comparison at all ten higher stations and the fixed pre-adoption
213-coordinate initial-bank witness (2/2, 34.992 s). The earlier combined
parts/source/trial-fixture run passes 9/9 (57.627 s). Indexed clearance,
two-sided locking, fixture parity and all nine coordinate-pair restraint laws
pass 5/5 (158.566 s). The recovered full profile matrix contains 23,556
admitted poses per station per kernel, 471,120 checks total, without a
positive common. Its separate evidence record distinguishes newly observed
terminal exits from historical logs; it is finite sampling, not a proof over
every continuous trajectory.

The production operating withdrawal run passes 2/2 (721.719 s) on framework
`70d0bd9`. Hundreds stops at 165.223238373° for requests to 190° and 880°;
station eight stops at 265.223238373° for requests to 290° and 980°.
Every stop has exactly zero complete-print native and faceted common, with
positive contact after .2° overtravel. Each case preserves the held shaft,
replays its snapshot exactly, permits -.05° relief and stops again on retry.
Four stopped/relieved full 213-coordinate banks are saved for independent
browser comparison. The [evidence index](evidence/result-bank-production-2026-09-22.json)
pins source and log hashes.

## Remaining gates

The inherited isolated trial's arithmetic and browser evidence is retained,
not relabelled as fresh acceptance of this expanded production root. Fresh
combined-root arithmetic, browser bank comparison and physical pointer
gestures remain to run. Normal operation is still too slow; independent
framework/viewer work is addressing measured evaluator costs without changing
the mechanism's laws, timestep or coordinate bank. Whole-machine positive
contacts, higher counters and clearing-loop deployment remain open. No
OpenSpec task is completed by this scoped adoption.
